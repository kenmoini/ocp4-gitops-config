---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: operator-serverless
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "05"
  labels:
    has-git-repo-objects: "true"
spec:
  destination:
    server: 'https://kubernetes.default.svc'
  sources:
    - repoURL: 'https://github.com/kenmoini/ocp4-gitops-config'
      targetRevision: main
      path: openshift/serverless-operator/operator/overlays/stable/
      #directory:
      #  recurse: true
      kustomize: {}
  project: local-cluster
  syncPolicy:
    automated:
      prune: false
      allowEmpty: true
      # selfHeal: false # overrides changes made on the cluster that drift from git state
